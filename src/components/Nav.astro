---
// yeet
const currentPath = Astro.url.pathname;
---

<nav class="navbar">
  <a href="/" class={`nav-link home${currentPath === '/' ? ' active' : ''}`} aria-disabled={currentPath === '/'} tabindex={currentPath === '/' ? -1 : undefined}>Home</a>
  <a href="/about" class={`nav-link about${currentPath === '/about' ? ' active' : ''}`} aria-disabled={currentPath === '/about'} tabindex={currentPath === '/about' ? -1 : undefined}>About</a>
  <a href="/projects" class={`nav-link projects${currentPath === '/projects' ? ' active' : ''}`} aria-disabled={currentPath === '/projects'} tabindex={currentPath === '/projects' ? -1 : undefined}>Projects</a>
  <a href="/contact" class={`nav-link contact${currentPath === '/contact' ? ' active' : ''}`} aria-disabled={currentPath === '/contact'} tabindex={currentPath === '/contact' ? -1 : undefined}>Contact + Extras 😉</a>
</nav>

<style lang="scss">
.navbar {
  display: flex;
  justify-content: center;
  gap: 2rem;
  padding: 1.5rem;
  background: #eee;
  font-family: 'IBM Plex Sans', sans-serif;
  box-shadow: 0 0px 17px #a38bbf3b;
}

.nav-link {
  text-decoration: none;
  color: #444;
  font-weight: bold;
  position: relative;
  transition: color 0.3s ease;

  &::after {
    content: '';
    display: block;
    height: 2px;
    width: 0%;
    transition: width 0.3s ease;
    background-color: currentColor;
    position: absolute;
    bottom: -4px;
    left: 0;
  }

  &:hover::after {
    width: 100%;
  }

  &.home:hover {
    color: #a084ca;
  }

  &.about:hover {
    color: #ff69b4;
  }

  &.projects:hover {
    color: #4caf50;
  }

  &.contact:hover {
    color: #2196f3;
  }

  &.active {
    pointer-events: none;
    opacity: 0.6;
    cursor: default;
  }
}
</style>
